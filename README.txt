Introduction
============

The `sixfeetup.deployment` package is a set of Fabric commands that help with deploying projects.

NOTE: This requires that you have `colordiff` installed if you want to view diffs.
